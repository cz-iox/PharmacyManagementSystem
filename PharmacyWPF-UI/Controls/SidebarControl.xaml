<UserControl x:Class="PharmacyWPF_UI.Controls.SidebarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PharmacyWPF_UI.Controls"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="220">
    <Grid Background="{StaticResource NavBarBrush}" >
        
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <iconPacks:PackIconMaterial Kind="Pill" Width="24" Height="24" Margin="10,0"/>
                <TextBlock Text="PHARMA CARE" 
                           Margin="10,0"
                           Foreground="{StaticResource TextPrimaryBrush}"
                           FontSize="20"
                           FontWeight="bold"
                           FontFamily="alexandria"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
